name: auto-test
permissions:
  contents: read

on: push

jobs:
  auto-test:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - uses: actions/checkout@v6
      - name: use Node.js
        uses: actions/setup-node@v6
        with:
          node-version: '24'
          cache: 'npm'
      - name: install dependency
        run: npm install
      - name: test format
        run: npm run format
      - name: test linter
        run: npm run lint
      - name: test compiler
        run: npm run tsc
